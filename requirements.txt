# Vector DB
chromadb>=0.6.0

# Embeddings (ONNX - no torch needed)
sentence-transformers==3.0.1

# LLM - OpenAI (primary) or Ollama (fallback)
openai>=1.0.0
ollama==0.3.0

# FastAPI backend
fastapi>=0.100.0
uvicorn>=0.18.3
python-multipart>=0.0.10
